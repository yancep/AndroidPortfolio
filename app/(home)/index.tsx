import { useRouter } from "expo-router";
import { View } from "react-native";

export default function TabTwoScreen() {
  const router = useRouter();

  return (
    <View className="w-full h-full bg-white container p-20">
    </View>
  );
}
